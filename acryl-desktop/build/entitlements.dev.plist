<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!--
      Local ad-hoc dev launch entitlements for the rebranded Electron.app
      copy `scripts/launch-dev.mjs` signs (see `prepareDarwinDevelopmentBundle`).
      Mirrors app-builder-lib's own release `entitlements.mac.plist`
      (node_modules/app-builder-lib/templates/entitlements.mac.plist), which
      the packaged `dist:mac` build already signs with successfully, plus the
      hardened-runtime inheritance the ad-hoc rebrand needs for its own
      Electron Helper.app bundles to complete the Mach IPC rendezvous with a
      renamed main app (otherwise every Helper looks up the wrong Mach
      service name and each renderer/GPU process exits immediately with
      `Unknown service name` / `No rendezvous client, terminating process`).
    -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <key>com.apple.security.inherit</key>
    <true/>
  </dict>
</plist>
