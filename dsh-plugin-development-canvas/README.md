# Development Canvas Cordis Plugin

Standalone Host and Client Cordis plugin for the ACR Development Canvas.

- Host owns loopback PTY routes and process lifetime.
- Client contributes the Canvas to the Desktop `desktop.main` slot.
- Removing the Loader row disposes both faces and restores the default conversation.
