# Keep workspace dependency graphs isolated. Published DSH client packages get
# their explicit React 18 type extensions from pnpm-workspace.yaml, while the
# TUI owns its independent React 19 graph.
node-linker=isolated
