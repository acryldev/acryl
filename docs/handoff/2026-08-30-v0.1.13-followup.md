# Handoff prompt — continue ACRYL from v0.1.13 (for the M1 MAX Hermes agent)

You are the Hermes/agent on **M1 MAX** continuing the ACRYL release work. The canonical
repo is at `~/ _projects/p11_acr_agentcontextrelay/acryldev/acryl` on this machine, and it is
now **fully synced to `origin/main`** (`695f3bb`). All the work below is already committed and
pushed; the local agent files and dirty submodule are preserved for you.

## Current state — the release is LIVE but npm is one step from done

**GitHub Release `v0.1.13` is PUBLISHED and green** (run `33322431850`, now `695f3bb` on main):
- CLI archives (5): `acryl-cli-darwin-arm64/x64.tar.gz`, `linux-arm64/x64.tar.gz`, `windows-x64.zip`
- Desktop installers (5): `desktop-mac-arm64/x64.dmg`, `linux-amd64/arm64.deb`, `win-x64.exe`
- `checksums.txt`
- The release was built by the combined `release.yml` (`build` + `cli` + `release` jobs), with the
  new `npm-publish` job gated on `startsWith(github.ref, 'refs/tags/v')`.

**What changed in this release cycle (all on `origin/main = 695f3bb`):**
- `3d12e82` external npm CLI installs a **bootable dependency closure** + `--help`
- `3d5273f` docs
- `b06bca5` bump workspace → 0.1.12
- `f3103a4` **release: auto-publish to npm + enforce version sync** (added `npm-publish` job + guards)
- `ba747f3` bump workspace → **0.1.13** (0.1.12 is already published/broken on npm, so 0.1.13 is the fresh version)
- `0725b83` fix grammar tests to expect `help:false`
- `c2a03ef` **remove invalid `secrets`-in-`if:` steps** from the release job (these made GitHub reject the workflow)
- `695f3bb` (release automation) sync README downloads for v0.1.13

**Version invariant (enforced by CI):** `git tag v<X>` ⇔ the five workspace `package.json` = `<X>`
⇔ npm `acryl` = `<X>`. The `npm-publish` job fails loudly on drift.

## THE ONLY REMAINING TASK: publish `acryl@0.1.13` to npm

The `npm-publish` job fails with:
`npm error 404 — PUT https://registry.npmjs.org/acryl — 'acryl@0.1.13' could not be found or you do not have permission to access it.`

Diagnosis already done:
- The publish script (`scripts/publish-npm-cli.mjs`) authenticates correctly (writes a `.npmrc` from
  `NPM_TOKEN` and runs `npm publish` with `NPM_CONFIG_USERCONFIG`). Auth plumbing is fine.
- The package `acryl` is owned/maintained by `musichen`, `webboxescom`, `acryldev`.
- **Both local tokens fail 404:** `~/.secure-storage/npm/npm.json` (`npm_9L…rSyH`, labelled `webboxescom`)
  and M1 MAX `~/.npmrc` (`npm_…7eVf`). They authenticate but the registry rejects them for `acryl` —
  i.e. they are scope-limited / stale / not the maintainer token, even though the `webboxescom` account
  has write access as a maintainer.

**Next step for you:**
1. Obtain a **valid maintainer-scoped npm publish token** for `musichen` / `webboxescom` / `acryldev`
   (40-char `npm_` + 36). Have the human regenerate the `webboxescom` token (or supply the exact one
   used to publish 0.1.12), or run `npm access grant read-write webboxescom:acryl` as an owner to be sure.
2. Set it: `gh secret set NPM_TOKEN --repo acryldev/acryl --body -` (read the value, do NOT echo it).
3. Re-run the npm-publish job: `gh run rerun <<latest_release_run_id>> --failed` (or the `npm-publish` job).
4. Verify: `npm view acryl version` → `0.1.13` (and `npm view acryl dist-tags.latest` = `0.1.13`).

**Do NOT** change the GitHub Release or re-cut `v0.1.13` — the binaries are already live and correct.
Only the npm package needs publishing to close the loop.

## Local M1 MAX state (preserve these — do not discard)
- Dirty: `deepseek-harness` submodule (→ `cd5ef81`, a local harness experiment), `upstream.json`, and
  `specs/019-acryl-harness-runtime/evidence/tui-pty-smoke.output.txt`.
- Untracked: `.codewhale/`, `.openclaude/`, `acryl-tui/lib-publish/`.
- The committed submodule pointer (release input) is `b150a55` (known-good); the dirty `cd5ef81` is a local experiment, not part of the release.

## Constraints
- Work directly on `main`; no routine PRs.
- `corepack pnpm` only (PNPM 11.7.0), Node v24.19.0 (nvm).
- Keep `deepseek-harness` pinned/read-only for releases.
- Record work in `docs/DEVELOPMENT-LOG.md` (prepend via patch, never `write_file` whole file).
- Follow `docs/workmethodology/acryl-hybrid-engineering-methodology.md`.
- GitHub push: do it from a machine with credentials (AION); M1 MAX's HTTPS remote has no cached creds.
