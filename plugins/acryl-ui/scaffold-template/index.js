// my-ui-plugin (host half). Rename the package first (package.json, tsdown.config.ts's
// PACKAGE_NAME); the Loader row id and the client module id both come from the package name.
export const name = 'my-ui-plugin'
