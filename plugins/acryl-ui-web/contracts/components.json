{
  "schemaVersion": 1,
  "library": "acryl-ui-web",
  "surfaces": [
    "web",
    "desktop",
    "tui"
  ],
  "components": {
    "Stack": {
      "summary": "Lays children out in a row or a column with a theme-consistent gap.",
      "props": {
        "direction": {
          "type": "'column' | 'row'",
          "default": "'column'"
        },
        "gap": {
          "type": "'xs' | 'sm' | 'md' | 'lg'",
          "default": "'md'"
        },
        "align": {
          "type": "'start' | 'center' | 'end' | 'stretch'",
          "default": "'stretch'"
        }
      },
      "children": true,
      "a11y": [
        "no role: it is layout only"
      ],
      "surfaces": [
        "web",
        "tui"
      ]
    },
    "Card": {
      "summary": "A bordered surface with an optional title, body and footer actions.",
      "props": {
        "title": {
          "type": "string"
        },
        "footer": {
          "type": "ReactNode",
          "description": "actions, right-aligned"
        }
      },
      "children": true,
      "a11y": [
        "role=group, labelled by the title when there is one"
      ],
      "surfaces": [
        "web",
        "tui"
      ]
    },
    "Field": {
      "summary": "A labelled text input with hint and error text.",
      "props": {
        "label": {
          "type": "string",
          "required": true
        },
        "value": {
          "type": "string",
          "required": true
        },
        "onChange": {
          "type": "(value: string) => void",
          "required": true,
          "description": "receives the string, not the event"
        },
        "hint": {
          "type": "string"
        },
        "error": {
          "type": "string",
          "description": "shown in place of the hint and announced"
        },
        "placeholder": {
          "type": "string"
        }
      },
      "a11y": [
        "the label is associated with the input",
        "the error is announced (role=alert) and referenced by aria-describedby"
      ],
      "surfaces": [
        "web",
        "tui"
      ]
    },
    "SwitchField": {
      "summary": "A switch with a label and optional hint.",
      "props": {
        "label": {
          "type": "string",
          "required": true
        },
        "checked": {
          "type": "boolean",
          "required": true
        },
        "onChange": {
          "type": "(checked: boolean) => void",
          "required": true
        },
        "hint": {
          "type": "string"
        }
      },
      "a11y": [
        "the label is the accessible name"
      ],
      "surfaces": [
        "web",
        "tui"
      ]
    },
    "EmptyState": {
      "summary": "Centered message for a list or view with nothing in it yet.",
      "props": {
        "title": {
          "type": "string",
          "required": true
        },
        "description": {
          "type": "string"
        },
        "action": {
          "type": "ReactNode"
        }
      },
      "a11y": [
        "the title is a heading"
      ],
      "surfaces": [
        "web",
        "tui"
      ]
    },
    "SettingsRow": {
      "summary": "A settings-style row: label and description on the left, one control on the right (the layout of the app's own Settings screens).",
      "props": {
        "label": {
          "type": "string",
          "required": true
        },
        "description": {
          "type": "string"
        },
        "focused": {
          "type": "boolean",
          "description": "terminal only: marks the row that has the keyboard focus"
        }
      },
      "children": true,
      "a11y": [
        "the control passed as children keeps its own accessible name"
      ],
      "surfaces": [
        "web",
        "tui"
      ]
    },
    "SelectField": {
      "summary": "A dropdown built on the app's own Menu: shows the selected option and opens the option list.",
      "props": {
        "options": {
          "type": "Array<{ id: string, label: string, disabled?: boolean }>",
          "required": true
        },
        "value": {
          "type": "string",
          "required": true
        },
        "onChange": {
          "type": "(id: string) => void",
          "required": true
        },
        "placeholder": {
          "type": "string",
          "default": "'Select'"
        },
        "label": {
          "type": "string",
          "description": "accessible name of the trigger"
        }
      },
      "a11y": [
        "trigger has aria-haspopup and aria-expanded",
        "Escape and outside click close it (Menu)"
      ],
      "surfaces": [
        "web",
        "tui"
      ]
    },
    "Segmented": {
      "summary": "Tile group for a small set of exclusive choices: DSH's Appearance picker (Light, Dark, System), a full-width row.",
      "props": {
        "options": {
          "type": "Array<{ id: string, label: string, icon?: ReactNode }>",
          "required": true
        },
        "value": {
          "type": "string",
          "required": true
        },
        "onChange": {
          "type": "(id: string) => void",
          "required": true
        },
        "label": {
          "type": "string",
          "description": "accessible name of the group"
        },
        "title": {
          "type": "string",
          "description": "visible title above the tiles (DSH shows one). The row is full width: do not put it inside a SettingsRow's right-hand control."
        }
      },
      "a11y": [
        "role=radiogroup with role=radio tiles and aria-checked",
        "roving tabindex; arrow keys move the choice"
      ],
      "surfaces": [
        "web",
        "tui"
      ]
    },
    "Tabs": {
      "summary": "Tab strip with the active panel as children.",
      "props": {
        "tabs": {
          "type": "Array<{ id: string, label: string }>",
          "required": true
        },
        "value": {
          "type": "string",
          "required": true
        },
        "onChange": {
          "type": "(id: string) => void",
          "required": true
        },
        "label": {
          "type": "string"
        }
      },
      "children": true,
      "a11y": [
        "role=tablist, tab and tabpanel wired with aria-controls and aria-labelledby",
        "roving tabindex; left and right arrows move the tab"
      ],
      "surfaces": [
        "web",
        "tui"
      ]
    },
    "Dialog": {
      "summary": "A confirm dialog over the app's Modal, with cancel and confirm actions.",
      "props": {
        "open": {
          "type": "boolean",
          "required": true
        },
        "title": {
          "type": "string",
          "required": true
        },
        "onClose": {
          "type": "() => void",
          "required": true
        },
        "onConfirm": {
          "type": "() => void",
          "required": true
        },
        "confirmLabel": {
          "type": "string",
          "default": "'OK'"
        },
        "cancelLabel": {
          "type": "string",
          "default": "'Cancel'"
        },
        "danger": {
          "type": "boolean",
          "default": "false",
          "description": "terminal only (paints the confirm action with the error role). On the web use the app's RiskConfirmation primitive for destructive confirmations: its Button has no danger variant."
        }
      },
      "children": true,
      "a11y": [
        "focus handling and Escape come from the app's Modal"
      ],
      "surfaces": [
        "web",
        "tui"
      ]
    },
    "SidebarRow": {
      "summary": "The sidebar's New Session bar: icon and label when the sidebar is wide, a plain icon control with a tooltip on the rail (extracted from DSH's sidebar).",
      "props": {
        "icon": {
          "type": "ReactNode",
          "required": true
        },
        "label": {
          "type": "string",
          "required": true,
          "description": "visible text when wide, tooltip and accessible name on the rail"
        },
        "wide": {
          "type": "boolean",
          "required": true
        },
        "onClick": {
          "type": "() => void",
          "required": true
        }
      },
      "surfaces": [
        "web"
      ],
      "a11y": [
        "aria-label carries the label in both forms"
      ]
    },
    "ToolCallCard": {
      "summary": "A collapsible tool-call row as in the conversation: leading icon or state dot, title, summary, expandable IN/OUT card (extracted from DSH's ToolRow).",
      "props": {
        "icon": {
          "type": "ReactNode",
          "required": true
        },
        "title": {
          "type": "string",
          "required": true
        },
        "summary": {
          "type": "string",
          "required": true
        },
        "state": {
          "type": "'running' | 'ok' | 'error' | 'stopped'",
          "required": true
        },
        "errorSummary": {
          "type": "string",
          "description": "replaces the summary on an error row"
        },
        "input": {
          "type": "string"
        },
        "output": {
          "type": "string"
        },
        "labels": {
          "type": "{ input, output, running, failed, stopped }",
          "required": true,
          "description": "copy; the run-state labels are visually hidden text"
        }
      },
      "children": true,
      "surfaces": [
        "web"
      ],
      "a11y": [
        "expandable rows are disclosure controls (Enter and Space toggle)",
        "the run state is exposed as visually hidden text, not colour only"
      ]
    }
  },
  "helpers": {
    "footerAction": {
      "summary": "Register a component in the sidebar footer (root scope, receives { wide }).",
      "slot": "sidebar.footer.action"
    },
    "headerAction": {
      "summary": "Register a component next to the conversation title (session scope).",
      "slot": "conversation.session.header.actions"
    },
    "sidebarTab": {
      "summary": "Register a tab in the right sidebar (session scope).",
      "slot": "sidebar.right.pane.tab"
    },
    "settingsSection": {
      "summary": "Add a page to the Settings screen (a nav entry beside General, Models, Plugins). Give `label` for the nav text.",
      "slot": "settings.section"
    }
  },
  "reexports": {
    "note": "Straight from the app's primitives so a consumer needs one import.",
    "names": [
      "Button",
      "Tag",
      "Pill",
      "Toast",
      "Modal",
      "Tooltip"
    ]
  },
  "roles": {
    "note": "Semantic role CSS variables (--acryl-<role>), mapped to the app tokens in contracts/tokens.json.",
    "source": "contracts/tokens.json"
  },
  "notes": {
    "tui": "The terminal library (plugins/acryl-ui-tui) implements every component with pi-tui: the props above, keyboard instead of pointer (arrow keys, space, enter, y/n, escape), and no line ever wider than the terminal."
  }
}
