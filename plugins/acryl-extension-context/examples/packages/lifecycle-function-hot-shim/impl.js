// The real plugin logic. Edit this file freely; re-install to update. Anything a plugin does in apply()
// (provide services, register tools, listen to events) belongs here.
export function apply(ctx) {
  ctx.logger.info('[acryl-example-hot-shim] version 1')
}
