# acryl-blend-demo

Inert Cordis function plugin that exists to be named by BLEND lock rows
(`blends.acryl.dev/v1alpha1`, desktop consumption spec 003). Mounting it logs
the row's locked `config` once; it provides no services, tools, or client
surface. Future `@acryl/*` product packages replace it as real BLEND payloads.
