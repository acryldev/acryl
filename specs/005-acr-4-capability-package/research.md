# Research: ACR-4 — Capability package format + loader

**Status**: Stub

Reuse ctx.dynamicCordisRunner. Add manifest, permissions, provenance.

Record facts this milestone's plan waits on. Prefer DSH source and
`docs/acr/ACR_DSH_GAP_ANALYSIS.md` over new abstractions.
